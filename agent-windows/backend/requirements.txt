# SecureOps Agent — Windows (CORE dependencies)
# Kompatibel: Windows 10/11 + Windows Server 2019/2022, Python 3.10-3.13
#
# CATATAN: pywinpty tidak ada di sini. Di-install terpisah oleh install.ps1
# sebagai optional dependency karena Python 3.14 belum punya wheel-nya.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-multipart>=0.0.9,<1.0
psutil>=5.9,<7.0
websockets>=12.0,<15.0
